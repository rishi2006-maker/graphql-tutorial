version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: graphql-devgrid-postgres
    restart: always
    environment:
      POSTGRES_DB: devgrid
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
    volumes:
      - devgrid-pg-data:/var/lib/postgresql/data

volumes:
  devgrid-pg-data:
